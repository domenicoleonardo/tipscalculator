<!-- res/values/themes.xml -->
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.TipsCalculator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/screen2</item>
        <item name="colorOnPrimary">@color/white</item> <!-- Cor do rótulo flutuante quando o campo está ativo -->
        <item name="colorOnSurface">@color/white</item> <!-- Cor do rótulo flutuante quando o campo está inativo -->
        <item name="android:statusBarColor" tools:targetApi="1">?attr/colorPrimaryVariant</item>
    </style>

    <style name="Theme.TipsCalculator" parent="Base.Theme.TipsCalculator" />
</resources>
